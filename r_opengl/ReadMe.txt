========================================================================
    DYNAMIC LINK LIBRARY : r_opengl-Projektübersicht
========================================================================

Der Anwendungs-Assistent hat diese r_opengl-DLL erstellt.  
Diese Datei enthält eine Übersicht des Inhalts der Dateien der
 r_opengl-Anwendung.


r_opengl.vcproj
    Dies ist die Hauptprojektdatei für VC++-Projekte, die vom Anwendungs-Assistenten
    erstellt wird. Sie enthält Informationen über die Version von Visual C++, mit der 
    die Datei generiert wurde, über die Plattformen, Konfigurationen und Projektfeatures,
    die mit dem Anwendungs-Assistenten ausgewählt wurden.

r_opengl.cpp
    Dies ist die wichtigste DLL-Quelldatei.

	Diese DLL exportiert keine Symbole, deshalb wird beim Erstellen 
	keine .lib-Datei generiert. Wenn das Projekt eine Projektabhängigkeit 
	eines anderen Projekts sein soll, müssen Sie Code hinzufügen, um 
	symbole aus der DLL zu exportieren, damit eine Exportbibliothek
	erstellt wird, oder Sie können die Eigenschaft Eingabebibliothek ignorieren in der 
	Eigenschaftenseite Allgemein in den Projekteigenschaften 
	auf Ja festlegen.

/////////////////////////////////////////////////////////////////////////////
Weitere Standarddateien:

StdAfx.h, StdAfx.cpp
    Mit diesen Dateien werden vorkompilierte Headerdateien (PCH)
    mit der Bezeichnung r_opengl.pch und eine vorkompilierte Typdatei mit der Bezeichnung StdAfx.obj erstellt.

/////////////////////////////////////////////////////////////////////////////
Weitere Hinweise:

Der Anwendungs-Assistent verwendet "TODO:"-Kommentare, um Teile des Quellcodes anzuzeigen, die hinzugefügt oder angepasst werden müssen.

/////////////////////////////////////////////////////////////////////////////
